<UserControl x:Class="PresentationMergeTool.PresentationLayer.Controls.HelpElementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PresentationMergeTool.PresentationLayer.Controls"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="60">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Image Name="Image" Source="{Binding ImageSource}" MaxWidth="70" Stretch="Uniform" Grid.Row="0" HorizontalAlignment="Center" />
        <TextBlock Text="{Binding Header}" Grid.Row="1" TextAlignment="Center" VerticalAlignment="Center"
                   TextWrapping="Wrap" />
    </Grid>
</UserControl>